// GENERATED by bindgen/godot_to_minc.py - do not edit.
// source: Godot extension_api.json (Godot Engine v4.3.stable.official)
// classes: Object, Node, CanvasItem, Node2D, Node3D, RefCounted, Camera3D, SceneTree, Shader, ShaderMaterial, BaseMaterial3D, GeometryInstance3D, MeshInstance3D, CSGBox3D, Sprite2D, Label, Timer, BoxMesh, SphereMesh, PlaneMesh, StandardMaterial3D, DirectionalLight3D, OmniLight3D, SpotLight3D, CSGSphere3D, CSGCylinder3D, Window, Input, Engine, Time, OS, JSON, FileAccess, DirAccess
// regenerate: python3 bindgen/godot_to_minc.py bindgen/extension_api.json --spec bindgen/bindings.json --outdir lib
//
// Importable module - engine class bindings over the core runtime.
// <class>_new() constructs via classdb_construct_object; each
// method wrapper caches its MethodBind on first call, then
// ptrcalls with marshalled args via the core runtime
// (gd_method, gd_ptrcall, gd_string_name_new, gd_destroy).
import godot_core;

// === Object ===

// Object_new() -> GdObject*
GdObject* object_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Object");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Object.set(property: StringName, value: Variant) -> void
void* object_set__bind;
bool object_set__ready;
void object_set(GdObject* self, GdStringName* p_property, GdVariant* p_value) {
    if !object_set__ready {
        object_set__bind = gd_method("Object", "set", 3776071444);
        object_set__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_property);
    a[1] = cast(void*, p_value);
    gd_ptrcall(object_set__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Object.get(property: StringName) -> Variant
void* object_get__bind;
bool object_get__ready;
void object_get(GdObject* self, GdStringName* p_property, GdVariant* r_ret) {
    if !object_get__ready {
        object_get__bind = gd_method("Object", "get", 2760726917);
        object_get__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_property);
    gd_ptrcall(object_get__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, r_ret));
    return;
}

// Object.connect(signal: StringName, callable: Callable, flags: int) -> enum::Error
void* object_connect__bind;
bool object_connect__ready;
i64 object_connect(GdObject* self, GdStringName* p_signal, GdCallable* p_callable, i64 p_flags) {
    if !object_connect__ready {
        object_connect__bind = gd_method("Object", "connect", 1518946055);
        object_connect__ready = true;
    }
    void*[3] a;
    a[0] = cast(void*, p_signal);
    a[1] = cast(void*, p_callable);
    a[2] = cast(void*, &p_flags);
    i64 r = 0;
    gd_ptrcall(object_connect__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// object_connect - default flags=0
i64 object_connect(GdObject* self, GdStringName* p_signal, GdCallable* p_callable) {
    return object_connect(self, p_signal, p_callable, 0);
}

// Object.disconnect(signal: StringName, callable: Callable) -> void
void* object_disconnect__bind;
bool object_disconnect__ready;
void object_disconnect(GdObject* self, GdStringName* p_signal, GdCallable* p_callable) {
    if !object_disconnect__ready {
        object_disconnect__bind = gd_method("Object", "disconnect", 1874754934);
        object_disconnect__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_signal);
    a[1] = cast(void*, p_callable);
    gd_ptrcall(object_disconnect__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Object.has_method(method: StringName) -> bool
void* object_has_method__bind;
bool object_has_method__ready;
u8 object_has_method(GdObject* self, GdStringName* p_method) {
    if !object_has_method__ready {
        object_has_method__bind = gd_method("Object", "has_method", 2619796661);
        object_has_method__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_method);
    u8 r = 0;
    gd_ptrcall(object_has_method__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Object.has_signal(signal: StringName) -> bool
void* object_has_signal__bind;
bool object_has_signal__ready;
u8 object_has_signal(GdObject* self, GdStringName* p_signal) {
    if !object_has_signal__ready {
        object_has_signal__bind = gd_method("Object", "has_signal", 2619796661);
        object_has_signal__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_signal);
    u8 r = 0;
    gd_ptrcall(object_has_signal__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Object.get_class() -> String
void* object_get_class__bind;
bool object_get_class__ready;
void object_get_class(GdObject* self, GdString* r_ret) {
    if !object_get_class__ready {
        object_get_class__bind = gd_method("Object", "get_class", 201670096);
        object_get_class__ready = true;
    }
    gd_ptrcall(object_get_class__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Object.is_class(class: String) -> bool
void* object_is_class__bind;
bool object_is_class__ready;
u8 object_is_class(GdObject* self, GdString* p_class) {
    if !object_is_class__ready {
        object_is_class__bind = gd_method("Object", "is_class", 3927539163);
        object_is_class__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_class);
    u8 r = 0;
    gd_ptrcall(object_is_class__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Object.set_meta(name: StringName, value: Variant) -> void
void* object_set_meta__bind;
bool object_set_meta__ready;
void object_set_meta(GdObject* self, GdStringName* p_name, GdVariant* p_value) {
    if !object_set_meta__ready {
        object_set_meta__bind = gd_method("Object", "set_meta", 3776071444);
        object_set_meta__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_name);
    a[1] = cast(void*, p_value);
    gd_ptrcall(object_set_meta__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Object.get_meta(name: StringName, default: Variant) -> Variant
void* object_get_meta__bind;
bool object_get_meta__ready;
void object_get_meta(GdObject* self, GdStringName* p_name, GdVariant* p_default, GdVariant* r_ret) {
    if !object_get_meta__ready {
        object_get_meta__bind = gd_method("Object", "get_meta", 3990617847);
        object_get_meta__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_name);
    a[1] = cast(void*, p_default);
    gd_ptrcall(object_get_meta__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, r_ret));
    return;
}

// object_get_meta - default default=null
void object_get_meta(GdObject* self, GdStringName* p_name, GdVariant* r_ret) {
    GdVariant _d1;
    object_get_meta(self, p_name, &_d1, r_ret);
    return;
}

// Object.has_meta(name: StringName) -> bool
void* object_has_meta__bind;
bool object_has_meta__ready;
u8 object_has_meta(GdObject* self, GdStringName* p_name) {
    if !object_has_meta__ready {
        object_has_meta__bind = gd_method("Object", "has_meta", 2619796661);
        object_has_meta__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_name);
    u8 r = 0;
    gd_ptrcall(object_has_meta__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Object.remove_meta(name: StringName) -> void
void* object_remove_meta__bind;
bool object_remove_meta__ready;
void object_remove_meta(GdObject* self, GdStringName* p_name) {
    if !object_remove_meta__ready {
        object_remove_meta__bind = gd_method("Object", "remove_meta", 3304788590);
        object_remove_meta__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_name);
    gd_ptrcall(object_remove_meta__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Object.get_instance_id() -> int
void* object_get_instance_id__bind;
bool object_get_instance_id__ready;
i64 object_get_instance_id(GdObject* self) {
    if !object_get_instance_id__ready {
        object_get_instance_id__bind = gd_method("Object", "get_instance_id", 3905245786);
        object_get_instance_id__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(object_get_instance_id__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// === Node ===

// Node_new() -> GdObject*
GdObject* node_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Node");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Node.set_name(name: String) -> void
void* node_set_name__bind;
bool node_set_name__ready;
void node_set_name(GdObject* self, GdString* p_name) {
    if !node_set_name__ready {
        node_set_name__bind = gd_method("Node", "set_name", 83702148);
        node_set_name__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_name);
    gd_ptrcall(node_set_name__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node.get_name() -> StringName
void* node_get_name__bind;
bool node_get_name__ready;
void node_get_name(GdObject* self, GdStringName* r_ret) {
    if !node_get_name__ready {
        node_get_name__bind = gd_method("Node", "get_name", 2002593661);
        node_get_name__ready = true;
    }
    gd_ptrcall(node_get_name__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node.add_child(node: Node, force_readable_name: bool, internal: enum::Node.InternalMode) -> void
void* node_add_child__bind;
bool node_add_child__ready;
void node_add_child(GdObject* self, GdObject* p_node, u8 p_force_readable_name, i64 p_internal) {
    if !node_add_child__ready {
        node_add_child__bind = gd_method("Node", "add_child", 3863233950);
        node_add_child__ready = true;
    }
    void*[3] a;
    a[0] = cast(void*, &p_node);
    a[1] = cast(void*, &p_force_readable_name);
    a[2] = cast(void*, &p_internal);
    gd_ptrcall(node_add_child__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// node_add_child - default internal=0
void node_add_child(GdObject* self, GdObject* p_node, u8 p_force_readable_name) {
    node_add_child(self, p_node, p_force_readable_name, 0);
    return;
}

// node_add_child - default force_readable_name=false, internal=0
void node_add_child(GdObject* self, GdObject* p_node) {
    node_add_child(self, p_node, 0, 0);
    return;
}

// Node.remove_child(node: Node) -> void
void* node_remove_child__bind;
bool node_remove_child__ready;
void node_remove_child(GdObject* self, GdObject* p_node) {
    if !node_remove_child__ready {
        node_remove_child__bind = gd_method("Node", "remove_child", 1078189570);
        node_remove_child__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_node);
    gd_ptrcall(node_remove_child__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node.get_child_count(include_internal: bool) -> int
void* node_get_child_count__bind;
bool node_get_child_count__ready;
i64 node_get_child_count(GdObject* self, u8 p_include_internal) {
    if !node_get_child_count__ready {
        node_get_child_count__bind = gd_method("Node", "get_child_count", 894402480);
        node_get_child_count__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_include_internal);
    i64 r = 0;
    gd_ptrcall(node_get_child_count__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// node_get_child_count - default include_internal=false
i64 node_get_child_count(GdObject* self) {
    return node_get_child_count(self, 0);
}

// Node.get_child(idx: int, include_internal: bool) -> Node
void* node_get_child__bind;
bool node_get_child__ready;
GdObject* node_get_child(GdObject* self, i64 p_idx, u8 p_include_internal) {
    if !node_get_child__ready {
        node_get_child__bind = gd_method("Node", "get_child", 541253412);
        node_get_child__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, &p_idx);
    a[1] = cast(void*, &p_include_internal);
    GdObject* r = null;
    gd_ptrcall(node_get_child__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// node_get_child - default include_internal=false
GdObject* node_get_child(GdObject* self, i64 p_idx) {
    return node_get_child(self, p_idx, 0);
}

// Node.get_parent() -> Node
void* node_get_parent__bind;
bool node_get_parent__ready;
GdObject* node_get_parent(GdObject* self) {
    if !node_get_parent__ready {
        node_get_parent__bind = gd_method("Node", "get_parent", 3160264692);
        node_get_parent__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(node_get_parent__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Node.get_node_or_null(path: NodePath) -> Node
void* node_get_node_or_null__bind;
bool node_get_node_or_null__ready;
GdObject* node_get_node_or_null(GdObject* self, GdNodePath* p_path) {
    if !node_get_node_or_null__ready {
        node_get_node_or_null__bind = gd_method("Node", "get_node_or_null", 2734337346);
        node_get_node_or_null__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_path);
    GdObject* r = null;
    gd_ptrcall(node_get_node_or_null__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Node.queue_free() -> void
void* node_queue_free__bind;
bool node_queue_free__ready;
void node_queue_free(GdObject* self) {
    if !node_queue_free__ready {
        node_queue_free__bind = gd_method("Node", "queue_free", 3218959716);
        node_queue_free__ready = true;
    }
    gd_ptrcall(node_queue_free__bind, cast(void*, self), null, null);
    return;
}

// Node.is_inside_tree() -> bool
void* node_is_inside_tree__bind;
bool node_is_inside_tree__ready;
u8 node_is_inside_tree(GdObject* self) {
    if !node_is_inside_tree__ready {
        node_is_inside_tree__bind = gd_method("Node", "is_inside_tree", 36873697);
        node_is_inside_tree__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(node_is_inside_tree__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Node.set_process(enable: bool) -> void
void* node_set_process__bind;
bool node_set_process__ready;
void node_set_process(GdObject* self, u8 p_enable) {
    if !node_set_process__ready {
        node_set_process__bind = gd_method("Node", "set_process", 2586408642);
        node_set_process__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_enable);
    gd_ptrcall(node_set_process__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node.set_physics_process(enable: bool) -> void
void* node_set_physics_process__bind;
bool node_set_physics_process__ready;
void node_set_physics_process(GdObject* self, u8 p_enable) {
    if !node_set_physics_process__ready {
        node_set_physics_process__bind = gd_method("Node", "set_physics_process", 2586408642);
        node_set_physics_process__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_enable);
    gd_ptrcall(node_set_physics_process__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node.set_process_mode(mode: enum::Node.ProcessMode) -> void
void* node_set_process_mode__bind;
bool node_set_process_mode__ready;
void node_set_process_mode(GdObject* self, i64 p_mode) {
    if !node_set_process_mode__ready {
        node_set_process_mode__bind = gd_method("Node", "set_process_mode", 1841290486);
        node_set_process_mode__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_mode);
    gd_ptrcall(node_set_process_mode__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node.get_tree() -> SceneTree
void* node_get_tree__bind;
bool node_get_tree__ready;
GdObject* node_get_tree(GdObject* self) {
    if !node_get_tree__ready {
        node_get_tree__bind = gd_method("Node", "get_tree", 2958820483);
        node_get_tree__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(node_get_tree__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Node.find_child(pattern: String, recursive: bool, owned: bool) -> Node
void* node_find_child__bind;
bool node_find_child__ready;
GdObject* node_find_child(GdObject* self, GdString* p_pattern, u8 p_recursive, u8 p_owned) {
    if !node_find_child__ready {
        node_find_child__bind = gd_method("Node", "find_child", 2008217037);
        node_find_child__ready = true;
    }
    void*[3] a;
    a[0] = cast(void*, p_pattern);
    a[1] = cast(void*, &p_recursive);
    a[2] = cast(void*, &p_owned);
    GdObject* r = null;
    gd_ptrcall(node_find_child__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// node_find_child - default owned=true
GdObject* node_find_child(GdObject* self, GdString* p_pattern, u8 p_recursive) {
    return node_find_child(self, p_pattern, p_recursive, 1);
}

// node_find_child - default recursive=true, owned=true
GdObject* node_find_child(GdObject* self, GdString* p_pattern) {
    return node_find_child(self, p_pattern, 1, 1);
}

// Node.add_to_group(group: StringName, persistent: bool) -> void
void* node_add_to_group__bind;
bool node_add_to_group__ready;
void node_add_to_group(GdObject* self, GdStringName* p_group, u8 p_persistent) {
    if !node_add_to_group__ready {
        node_add_to_group__bind = gd_method("Node", "add_to_group", 3683006648);
        node_add_to_group__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_group);
    a[1] = cast(void*, &p_persistent);
    gd_ptrcall(node_add_to_group__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// node_add_to_group - default persistent=false
void node_add_to_group(GdObject* self, GdStringName* p_group) {
    node_add_to_group(self, p_group, 0);
    return;
}

// Node.remove_from_group(group: StringName) -> void
void* node_remove_from_group__bind;
bool node_remove_from_group__ready;
void node_remove_from_group(GdObject* self, GdStringName* p_group) {
    if !node_remove_from_group__ready {
        node_remove_from_group__bind = gd_method("Node", "remove_from_group", 3304788590);
        node_remove_from_group__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_group);
    gd_ptrcall(node_remove_from_group__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node.is_in_group(group: StringName) -> bool
void* node_is_in_group__bind;
bool node_is_in_group__ready;
u8 node_is_in_group(GdObject* self, GdStringName* p_group) {
    if !node_is_in_group__ready {
        node_is_in_group__bind = gd_method("Node", "is_in_group", 2619796661);
        node_is_in_group__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_group);
    u8 r = 0;
    gd_ptrcall(node_is_in_group__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Node.get_index(include_internal: bool) -> int
void* node_get_index__bind;
bool node_get_index__ready;
i64 node_get_index(GdObject* self, u8 p_include_internal) {
    if !node_get_index__ready {
        node_get_index__bind = gd_method("Node", "get_index", 894402480);
        node_get_index__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_include_internal);
    i64 r = 0;
    gd_ptrcall(node_get_index__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// node_get_index - default include_internal=false
i64 node_get_index(GdObject* self) {
    return node_get_index(self, 0);
}

// Node.reparent(new_parent: Node, keep_global_transform: bool) -> void
void* node_reparent__bind;
bool node_reparent__ready;
void node_reparent(GdObject* self, GdObject* p_new_parent, u8 p_keep_global_transform) {
    if !node_reparent__ready {
        node_reparent__bind = gd_method("Node", "reparent", 3685795103);
        node_reparent__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, &p_new_parent);
    a[1] = cast(void*, &p_keep_global_transform);
    gd_ptrcall(node_reparent__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// node_reparent - default keep_global_transform=true
void node_reparent(GdObject* self, GdObject* p_new_parent) {
    node_reparent(self, p_new_parent, 1);
    return;
}

// === CanvasItem ===

// CanvasItem_new() -> GdObject*
GdObject* canvasitem_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "CanvasItem");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// CanvasItem.set_visible(visible: bool) -> void
void* canvasitem_set_visible__bind;
bool canvasitem_set_visible__ready;
void canvasitem_set_visible(GdObject* self, u8 p_visible) {
    if !canvasitem_set_visible__ready {
        canvasitem_set_visible__bind = gd_method("CanvasItem", "set_visible", 2586408642);
        canvasitem_set_visible__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_visible);
    gd_ptrcall(canvasitem_set_visible__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CanvasItem.is_visible() -> bool
void* canvasitem_is_visible__bind;
bool canvasitem_is_visible__ready;
u8 canvasitem_is_visible(GdObject* self) {
    if !canvasitem_is_visible__ready {
        canvasitem_is_visible__bind = gd_method("CanvasItem", "is_visible", 36873697);
        canvasitem_is_visible__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(canvasitem_is_visible__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// CanvasItem.queue_redraw() -> void
void* canvasitem_queue_redraw__bind;
bool canvasitem_queue_redraw__ready;
void canvasitem_queue_redraw(GdObject* self) {
    if !canvasitem_queue_redraw__ready {
        canvasitem_queue_redraw__bind = gd_method("CanvasItem", "queue_redraw", 3218959716);
        canvasitem_queue_redraw__ready = true;
    }
    gd_ptrcall(canvasitem_queue_redraw__bind, cast(void*, self), null, null);
    return;
}

// CanvasItem.set_modulate(modulate: Color) -> void
void* canvasitem_set_modulate__bind;
bool canvasitem_set_modulate__ready;
void canvasitem_set_modulate(GdObject* self, GdColor* p_modulate) {
    if !canvasitem_set_modulate__ready {
        canvasitem_set_modulate__bind = gd_method("CanvasItem", "set_modulate", 2920490490);
        canvasitem_set_modulate__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_modulate);
    gd_ptrcall(canvasitem_set_modulate__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CanvasItem.get_modulate() -> Color
void* canvasitem_get_modulate__bind;
bool canvasitem_get_modulate__ready;
void canvasitem_get_modulate(GdObject* self, GdColor* r_ret) {
    if !canvasitem_get_modulate__ready {
        canvasitem_get_modulate__bind = gd_method("CanvasItem", "get_modulate", 3444240500);
        canvasitem_get_modulate__ready = true;
    }
    gd_ptrcall(canvasitem_get_modulate__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// CanvasItem.set_self_modulate(self_modulate: Color) -> void
void* canvasitem_set_self_modulate__bind;
bool canvasitem_set_self_modulate__ready;
void canvasitem_set_self_modulate(GdObject* self, GdColor* p_self_modulate) {
    if !canvasitem_set_self_modulate__ready {
        canvasitem_set_self_modulate__bind = gd_method("CanvasItem", "set_self_modulate", 2920490490);
        canvasitem_set_self_modulate__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_self_modulate);
    gd_ptrcall(canvasitem_set_self_modulate__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CanvasItem.set_z_index(z_index: int) -> void
void* canvasitem_set_z_index__bind;
bool canvasitem_set_z_index__ready;
void canvasitem_set_z_index(GdObject* self, i64 p_z_index) {
    if !canvasitem_set_z_index__ready {
        canvasitem_set_z_index__bind = gd_method("CanvasItem", "set_z_index", 1286410249);
        canvasitem_set_z_index__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_z_index);
    gd_ptrcall(canvasitem_set_z_index__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CanvasItem.get_z_index() -> int
void* canvasitem_get_z_index__bind;
bool canvasitem_get_z_index__ready;
i64 canvasitem_get_z_index(GdObject* self) {
    if !canvasitem_get_z_index__ready {
        canvasitem_get_z_index__bind = gd_method("CanvasItem", "get_z_index", 3905245786);
        canvasitem_get_z_index__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(canvasitem_get_z_index__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// CanvasItem.get_transform() -> Transform2D
void* canvasitem_get_transform__bind;
bool canvasitem_get_transform__ready;
void canvasitem_get_transform(GdObject* self, GdTransform2D* r_ret) {
    if !canvasitem_get_transform__ready {
        canvasitem_get_transform__bind = gd_method("CanvasItem", "get_transform", 3814499831);
        canvasitem_get_transform__ready = true;
    }
    gd_ptrcall(canvasitem_get_transform__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// === Node2D ===

// Node2D_new() -> GdObject*
GdObject* node2d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Node2D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Node2D.set_position(position: Vector2) -> void
void* node2d_set_position__bind;
bool node2d_set_position__ready;
void node2d_set_position(GdObject* self, GdVector2* p_position) {
    if !node2d_set_position__ready {
        node2d_set_position__bind = gd_method("Node2D", "set_position", 743155724);
        node2d_set_position__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_position);
    gd_ptrcall(node2d_set_position__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node2D.get_position() -> Vector2
void* node2d_get_position__bind;
bool node2d_get_position__ready;
void node2d_get_position(GdObject* self, GdVector2* r_ret) {
    if !node2d_get_position__ready {
        node2d_get_position__bind = gd_method("Node2D", "get_position", 3341600327);
        node2d_get_position__ready = true;
    }
    gd_ptrcall(node2d_get_position__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node2D.set_rotation(radians: float) -> void
void* node2d_set_rotation__bind;
bool node2d_set_rotation__ready;
void node2d_set_rotation(GdObject* self, f64 p_radians) {
    if !node2d_set_rotation__ready {
        node2d_set_rotation__bind = gd_method("Node2D", "set_rotation", 373806689);
        node2d_set_rotation__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_radians);
    gd_ptrcall(node2d_set_rotation__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node2D.get_rotation() -> float
void* node2d_get_rotation__bind;
bool node2d_get_rotation__ready;
f64 node2d_get_rotation(GdObject* self) {
    if !node2d_get_rotation__ready {
        node2d_get_rotation__bind = gd_method("Node2D", "get_rotation", 1740695150);
        node2d_get_rotation__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(node2d_get_rotation__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Node2D.set_scale(scale: Vector2) -> void
void* node2d_set_scale__bind;
bool node2d_set_scale__ready;
void node2d_set_scale(GdObject* self, GdVector2* p_scale) {
    if !node2d_set_scale__ready {
        node2d_set_scale__bind = gd_method("Node2D", "set_scale", 743155724);
        node2d_set_scale__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_scale);
    gd_ptrcall(node2d_set_scale__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node2D.get_scale() -> Vector2
void* node2d_get_scale__bind;
bool node2d_get_scale__ready;
void node2d_get_scale(GdObject* self, GdVector2* r_ret) {
    if !node2d_get_scale__ready {
        node2d_get_scale__bind = gd_method("Node2D", "get_scale", 3341600327);
        node2d_get_scale__ready = true;
    }
    gd_ptrcall(node2d_get_scale__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node2D.set_global_position(position: Vector2) -> void
void* node2d_set_global_position__bind;
bool node2d_set_global_position__ready;
void node2d_set_global_position(GdObject* self, GdVector2* p_position) {
    if !node2d_set_global_position__ready {
        node2d_set_global_position__bind = gd_method("Node2D", "set_global_position", 743155724);
        node2d_set_global_position__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_position);
    gd_ptrcall(node2d_set_global_position__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node2D.get_global_position() -> Vector2
void* node2d_get_global_position__bind;
bool node2d_get_global_position__ready;
void node2d_get_global_position(GdObject* self, GdVector2* r_ret) {
    if !node2d_get_global_position__ready {
        node2d_get_global_position__bind = gd_method("Node2D", "get_global_position", 3341600327);
        node2d_get_global_position__ready = true;
    }
    gd_ptrcall(node2d_get_global_position__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node2D.translate(offset: Vector2) -> void
void* node2d_translate__bind;
bool node2d_translate__ready;
void node2d_translate(GdObject* self, GdVector2* p_offset) {
    if !node2d_translate__ready {
        node2d_translate__bind = gd_method("Node2D", "translate", 743155724);
        node2d_translate__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_offset);
    gd_ptrcall(node2d_translate__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node2D.rotate(radians: float) -> void
void* node2d_rotate__bind;
bool node2d_rotate__ready;
void node2d_rotate(GdObject* self, f64 p_radians) {
    if !node2d_rotate__ready {
        node2d_rotate__bind = gd_method("Node2D", "rotate", 373806689);
        node2d_rotate__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_radians);
    gd_ptrcall(node2d_rotate__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node2D.move_local_x(delta: float, scaled: bool) -> void
void* node2d_move_local_x__bind;
bool node2d_move_local_x__ready;
void node2d_move_local_x(GdObject* self, f64 p_delta, u8 p_scaled) {
    if !node2d_move_local_x__ready {
        node2d_move_local_x__bind = gd_method("Node2D", "move_local_x", 2087892650);
        node2d_move_local_x__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, &p_delta);
    a[1] = cast(void*, &p_scaled);
    gd_ptrcall(node2d_move_local_x__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// node2d_move_local_x - default scaled=false
void node2d_move_local_x(GdObject* self, f64 p_delta) {
    node2d_move_local_x(self, p_delta, 0);
    return;
}

// Node2D.move_local_y(delta: float, scaled: bool) -> void
void* node2d_move_local_y__bind;
bool node2d_move_local_y__ready;
void node2d_move_local_y(GdObject* self, f64 p_delta, u8 p_scaled) {
    if !node2d_move_local_y__ready {
        node2d_move_local_y__bind = gd_method("Node2D", "move_local_y", 2087892650);
        node2d_move_local_y__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, &p_delta);
    a[1] = cast(void*, &p_scaled);
    gd_ptrcall(node2d_move_local_y__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// node2d_move_local_y - default scaled=false
void node2d_move_local_y(GdObject* self, f64 p_delta) {
    node2d_move_local_y(self, p_delta, 0);
    return;
}

// Node2D.look_at(point: Vector2) -> void
void* node2d_look_at__bind;
bool node2d_look_at__ready;
void node2d_look_at(GdObject* self, GdVector2* p_point) {
    if !node2d_look_at__ready {
        node2d_look_at__bind = gd_method("Node2D", "look_at", 743155724);
        node2d_look_at__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_point);
    gd_ptrcall(node2d_look_at__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node2D.set_transform(xform: Transform2D) -> void
void* node2d_set_transform__bind;
bool node2d_set_transform__ready;
void node2d_set_transform(GdObject* self, GdTransform2D* p_xform) {
    if !node2d_set_transform__ready {
        node2d_set_transform__bind = gd_method("Node2D", "set_transform", 2761652528);
        node2d_set_transform__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_xform);
    gd_ptrcall(node2d_set_transform__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === Node3D ===

// Node3D_new() -> GdObject*
GdObject* node3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Node3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Node3D.set_position(position: Vector3) -> void
void* node3d_set_position__bind;
bool node3d_set_position__ready;
void node3d_set_position(GdObject* self, GdVector3* p_position) {
    if !node3d_set_position__ready {
        node3d_set_position__bind = gd_method("Node3D", "set_position", 3460891852);
        node3d_set_position__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_position);
    gd_ptrcall(node3d_set_position__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.get_position() -> Vector3
void* node3d_get_position__bind;
bool node3d_get_position__ready;
void node3d_get_position(GdObject* self, GdVector3* r_ret) {
    if !node3d_get_position__ready {
        node3d_get_position__bind = gd_method("Node3D", "get_position", 3360562783);
        node3d_get_position__ready = true;
    }
    gd_ptrcall(node3d_get_position__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node3D.set_rotation(euler_radians: Vector3) -> void
void* node3d_set_rotation__bind;
bool node3d_set_rotation__ready;
void node3d_set_rotation(GdObject* self, GdVector3* p_euler_radians) {
    if !node3d_set_rotation__ready {
        node3d_set_rotation__bind = gd_method("Node3D", "set_rotation", 3460891852);
        node3d_set_rotation__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_euler_radians);
    gd_ptrcall(node3d_set_rotation__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.get_rotation() -> Vector3
void* node3d_get_rotation__bind;
bool node3d_get_rotation__ready;
void node3d_get_rotation(GdObject* self, GdVector3* r_ret) {
    if !node3d_get_rotation__ready {
        node3d_get_rotation__bind = gd_method("Node3D", "get_rotation", 3360562783);
        node3d_get_rotation__ready = true;
    }
    gd_ptrcall(node3d_get_rotation__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node3D.set_rotation_degrees(euler_degrees: Vector3) -> void
void* node3d_set_rotation_degrees__bind;
bool node3d_set_rotation_degrees__ready;
void node3d_set_rotation_degrees(GdObject* self, GdVector3* p_euler_degrees) {
    if !node3d_set_rotation_degrees__ready {
        node3d_set_rotation_degrees__bind = gd_method("Node3D", "set_rotation_degrees", 3460891852);
        node3d_set_rotation_degrees__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_euler_degrees);
    gd_ptrcall(node3d_set_rotation_degrees__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.get_rotation_degrees() -> Vector3
void* node3d_get_rotation_degrees__bind;
bool node3d_get_rotation_degrees__ready;
void node3d_get_rotation_degrees(GdObject* self, GdVector3* r_ret) {
    if !node3d_get_rotation_degrees__ready {
        node3d_get_rotation_degrees__bind = gd_method("Node3D", "get_rotation_degrees", 3360562783);
        node3d_get_rotation_degrees__ready = true;
    }
    gd_ptrcall(node3d_get_rotation_degrees__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node3D.set_scale(scale: Vector3) -> void
void* node3d_set_scale__bind;
bool node3d_set_scale__ready;
void node3d_set_scale(GdObject* self, GdVector3* p_scale) {
    if !node3d_set_scale__ready {
        node3d_set_scale__bind = gd_method("Node3D", "set_scale", 3460891852);
        node3d_set_scale__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_scale);
    gd_ptrcall(node3d_set_scale__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.get_scale() -> Vector3
void* node3d_get_scale__bind;
bool node3d_get_scale__ready;
void node3d_get_scale(GdObject* self, GdVector3* r_ret) {
    if !node3d_get_scale__ready {
        node3d_get_scale__bind = gd_method("Node3D", "get_scale", 3360562783);
        node3d_get_scale__ready = true;
    }
    gd_ptrcall(node3d_get_scale__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node3D.set_global_position(position: Vector3) -> void
void* node3d_set_global_position__bind;
bool node3d_set_global_position__ready;
void node3d_set_global_position(GdObject* self, GdVector3* p_position) {
    if !node3d_set_global_position__ready {
        node3d_set_global_position__bind = gd_method("Node3D", "set_global_position", 3460891852);
        node3d_set_global_position__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_position);
    gd_ptrcall(node3d_set_global_position__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.get_global_position() -> Vector3
void* node3d_get_global_position__bind;
bool node3d_get_global_position__ready;
void node3d_get_global_position(GdObject* self, GdVector3* r_ret) {
    if !node3d_get_global_position__ready {
        node3d_get_global_position__bind = gd_method("Node3D", "get_global_position", 3360562783);
        node3d_get_global_position__ready = true;
    }
    gd_ptrcall(node3d_get_global_position__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node3D.set_transform(local: Transform3D) -> void
void* node3d_set_transform__bind;
bool node3d_set_transform__ready;
void node3d_set_transform(GdObject* self, GdTransform3D* p_local) {
    if !node3d_set_transform__ready {
        node3d_set_transform__bind = gd_method("Node3D", "set_transform", 2952846383);
        node3d_set_transform__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_local);
    gd_ptrcall(node3d_set_transform__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.get_transform() -> Transform3D
void* node3d_get_transform__bind;
bool node3d_get_transform__ready;
void node3d_get_transform(GdObject* self, GdTransform3D* r_ret) {
    if !node3d_get_transform__ready {
        node3d_get_transform__bind = gd_method("Node3D", "get_transform", 3229777777);
        node3d_get_transform__ready = true;
    }
    gd_ptrcall(node3d_get_transform__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node3D.set_global_transform(global: Transform3D) -> void
void* node3d_set_global_transform__bind;
bool node3d_set_global_transform__ready;
void node3d_set_global_transform(GdObject* self, GdTransform3D* p_global) {
    if !node3d_set_global_transform__ready {
        node3d_set_global_transform__bind = gd_method("Node3D", "set_global_transform", 2952846383);
        node3d_set_global_transform__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_global);
    gd_ptrcall(node3d_set_global_transform__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.get_global_transform() -> Transform3D
void* node3d_get_global_transform__bind;
bool node3d_get_global_transform__ready;
void node3d_get_global_transform(GdObject* self, GdTransform3D* r_ret) {
    if !node3d_get_global_transform__ready {
        node3d_get_global_transform__bind = gd_method("Node3D", "get_global_transform", 3229777777);
        node3d_get_global_transform__ready = true;
    }
    gd_ptrcall(node3d_get_global_transform__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// Node3D.look_at(target: Vector3, up: Vector3, use_model_front: bool) -> void
void* node3d_look_at__bind;
bool node3d_look_at__ready;
void node3d_look_at(GdObject* self, GdVector3* p_target, GdVector3* p_up, u8 p_use_model_front) {
    if !node3d_look_at__ready {
        node3d_look_at__bind = gd_method("Node3D", "look_at", 2882425029);
        node3d_look_at__ready = true;
    }
    void*[3] a;
    a[0] = cast(void*, p_target);
    a[1] = cast(void*, p_up);
    a[2] = cast(void*, &p_use_model_front);
    gd_ptrcall(node3d_look_at__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// node3d_look_at - default use_model_front=false
void node3d_look_at(GdObject* self, GdVector3* p_target, GdVector3* p_up) {
    node3d_look_at(self, p_target, p_up, 0);
    return;
}

// node3d_look_at - default up=Vector3(0, 1, 0), use_model_front=false
void node3d_look_at(GdObject* self, GdVector3* p_target) {
    GdVector3 _d1 = GdVector3{ 0.0f, 1.0f, 0.0f };
    node3d_look_at(self, p_target, &_d1, 0);
    return;
}

// Node3D.rotate_x(angle: float) -> void
void* node3d_rotate_x__bind;
bool node3d_rotate_x__ready;
void node3d_rotate_x(GdObject* self, f64 p_angle) {
    if !node3d_rotate_x__ready {
        node3d_rotate_x__bind = gd_method("Node3D", "rotate_x", 373806689);
        node3d_rotate_x__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_angle);
    gd_ptrcall(node3d_rotate_x__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.rotate_y(angle: float) -> void
void* node3d_rotate_y__bind;
bool node3d_rotate_y__ready;
void node3d_rotate_y(GdObject* self, f64 p_angle) {
    if !node3d_rotate_y__ready {
        node3d_rotate_y__bind = gd_method("Node3D", "rotate_y", 373806689);
        node3d_rotate_y__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_angle);
    gd_ptrcall(node3d_rotate_y__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.rotate_z(angle: float) -> void
void* node3d_rotate_z__bind;
bool node3d_rotate_z__ready;
void node3d_rotate_z(GdObject* self, f64 p_angle) {
    if !node3d_rotate_z__ready {
        node3d_rotate_z__bind = gd_method("Node3D", "rotate_z", 373806689);
        node3d_rotate_z__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_angle);
    gd_ptrcall(node3d_rotate_z__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.translate(offset: Vector3) -> void
void* node3d_translate__bind;
bool node3d_translate__ready;
void node3d_translate(GdObject* self, GdVector3* p_offset) {
    if !node3d_translate__ready {
        node3d_translate__bind = gd_method("Node3D", "translate", 3460891852);
        node3d_translate__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_offset);
    gd_ptrcall(node3d_translate__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.set_visible(visible: bool) -> void
void* node3d_set_visible__bind;
bool node3d_set_visible__ready;
void node3d_set_visible(GdObject* self, u8 p_visible) {
    if !node3d_set_visible__ready {
        node3d_set_visible__bind = gd_method("Node3D", "set_visible", 2586408642);
        node3d_set_visible__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_visible);
    gd_ptrcall(node3d_set_visible__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Node3D.is_visible() -> bool
void* node3d_is_visible__bind;
bool node3d_is_visible__ready;
u8 node3d_is_visible(GdObject* self) {
    if !node3d_is_visible__ready {
        node3d_is_visible__bind = gd_method("Node3D", "is_visible", 36873697);
        node3d_is_visible__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(node3d_is_visible__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// === RefCounted ===

// RefCounted_new() -> GdObject*
GdObject* refcounted_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "RefCounted");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// RefCounted.init_ref() -> bool
void* refcounted_init_ref__bind;
bool refcounted_init_ref__ready;
u8 refcounted_init_ref(GdObject* self) {
    if !refcounted_init_ref__ready {
        refcounted_init_ref__bind = gd_method("RefCounted", "init_ref", 2240911060);
        refcounted_init_ref__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(refcounted_init_ref__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// RefCounted.unreference() -> bool
void* refcounted_unreference__bind;
bool refcounted_unreference__ready;
u8 refcounted_unreference(GdObject* self) {
    if !refcounted_unreference__ready {
        refcounted_unreference__bind = gd_method("RefCounted", "unreference", 2240911060);
        refcounted_unreference__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(refcounted_unreference__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// RefCounted.reference() -> bool
void* refcounted_reference__bind;
bool refcounted_reference__ready;
u8 refcounted_reference(GdObject* self) {
    if !refcounted_reference__ready {
        refcounted_reference__bind = gd_method("RefCounted", "reference", 2240911060);
        refcounted_reference__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(refcounted_reference__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// === Camera3D ===

// Camera3D_new() -> GdObject*
GdObject* camera3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Camera3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Camera3D.make_current() -> void
void* camera3d_make_current__bind;
bool camera3d_make_current__ready;
void camera3d_make_current(GdObject* self) {
    if !camera3d_make_current__ready {
        camera3d_make_current__bind = gd_method("Camera3D", "make_current", 3218959716);
        camera3d_make_current__ready = true;
    }
    gd_ptrcall(camera3d_make_current__bind, cast(void*, self), null, null);
    return;
}

// Camera3D.clear_current(enable_next: bool) -> void
void* camera3d_clear_current__bind;
bool camera3d_clear_current__ready;
void camera3d_clear_current(GdObject* self, u8 p_enable_next) {
    if !camera3d_clear_current__ready {
        camera3d_clear_current__bind = gd_method("Camera3D", "clear_current", 3216645846);
        camera3d_clear_current__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_enable_next);
    gd_ptrcall(camera3d_clear_current__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// camera3d_clear_current - default enable_next=true
void camera3d_clear_current(GdObject* self) {
    camera3d_clear_current(self, 1);
    return;
}

// Camera3D.set_fov(fov: float) -> void
void* camera3d_set_fov__bind;
bool camera3d_set_fov__ready;
void camera3d_set_fov(GdObject* self, f64 p_fov) {
    if !camera3d_set_fov__ready {
        camera3d_set_fov__bind = gd_method("Camera3D", "set_fov", 373806689);
        camera3d_set_fov__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_fov);
    gd_ptrcall(camera3d_set_fov__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Camera3D.get_fov() -> float
void* camera3d_get_fov__bind;
bool camera3d_get_fov__ready;
f64 camera3d_get_fov(GdObject* self) {
    if !camera3d_get_fov__ready {
        camera3d_get_fov__bind = gd_method("Camera3D", "get_fov", 1740695150);
        camera3d_get_fov__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(camera3d_get_fov__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Camera3D.set_near(near: float) -> void
void* camera3d_set_near__bind;
bool camera3d_set_near__ready;
void camera3d_set_near(GdObject* self, f64 p_near) {
    if !camera3d_set_near__ready {
        camera3d_set_near__bind = gd_method("Camera3D", "set_near", 373806689);
        camera3d_set_near__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_near);
    gd_ptrcall(camera3d_set_near__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Camera3D.get_near() -> float
void* camera3d_get_near__bind;
bool camera3d_get_near__ready;
f64 camera3d_get_near(GdObject* self) {
    if !camera3d_get_near__ready {
        camera3d_get_near__bind = gd_method("Camera3D", "get_near", 1740695150);
        camera3d_get_near__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(camera3d_get_near__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Camera3D.set_far(far: float) -> void
void* camera3d_set_far__bind;
bool camera3d_set_far__ready;
void camera3d_set_far(GdObject* self, f64 p_far) {
    if !camera3d_set_far__ready {
        camera3d_set_far__bind = gd_method("Camera3D", "set_far", 373806689);
        camera3d_set_far__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_far);
    gd_ptrcall(camera3d_set_far__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Camera3D.get_far() -> float
void* camera3d_get_far__bind;
bool camera3d_get_far__ready;
f64 camera3d_get_far(GdObject* self) {
    if !camera3d_get_far__ready {
        camera3d_get_far__bind = gd_method("Camera3D", "get_far", 1740695150);
        camera3d_get_far__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(camera3d_get_far__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Camera3D.set_perspective(fov: float, z_near: float, z_far: float) -> void
void* camera3d_set_perspective__bind;
bool camera3d_set_perspective__ready;
void camera3d_set_perspective(GdObject* self, f64 p_fov, f64 p_z_near, f64 p_z_far) {
    if !camera3d_set_perspective__ready {
        camera3d_set_perspective__bind = gd_method("Camera3D", "set_perspective", 2385087082);
        camera3d_set_perspective__ready = true;
    }
    void*[3] a;
    a[0] = cast(void*, &p_fov);
    a[1] = cast(void*, &p_z_near);
    a[2] = cast(void*, &p_z_far);
    gd_ptrcall(camera3d_set_perspective__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === SceneTree ===

// SceneTree_new() -> GdObject*
GdObject* scenetree_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "SceneTree");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// SceneTree.get_root() -> Window
void* scenetree_get_root__bind;
bool scenetree_get_root__ready;
GdObject* scenetree_get_root(GdObject* self) {
    if !scenetree_get_root__ready {
        scenetree_get_root__bind = gd_method("SceneTree", "get_root", 1757182445);
        scenetree_get_root__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(scenetree_get_root__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// SceneTree.get_frame() -> int
void* scenetree_get_frame__bind;
bool scenetree_get_frame__ready;
i64 scenetree_get_frame(GdObject* self) {
    if !scenetree_get_frame__ready {
        scenetree_get_frame__bind = gd_method("SceneTree", "get_frame", 3905245786);
        scenetree_get_frame__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(scenetree_get_frame__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// SceneTree.set_pause(enable: bool) -> void
void* scenetree_set_pause__bind;
bool scenetree_set_pause__ready;
void scenetree_set_pause(GdObject* self, u8 p_enable) {
    if !scenetree_set_pause__ready {
        scenetree_set_pause__bind = gd_method("SceneTree", "set_pause", 2586408642);
        scenetree_set_pause__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_enable);
    gd_ptrcall(scenetree_set_pause__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// SceneTree.is_paused() -> bool
void* scenetree_is_paused__bind;
bool scenetree_is_paused__ready;
u8 scenetree_is_paused(GdObject* self) {
    if !scenetree_is_paused__ready {
        scenetree_is_paused__bind = gd_method("SceneTree", "is_paused", 36873697);
        scenetree_is_paused__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(scenetree_is_paused__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// SceneTree.create_timer(time_sec: float, process_always: bool, process_in_physics: bool, ignore_time_scale: bool) -> SceneTreeTimer
void* scenetree_create_timer__bind;
bool scenetree_create_timer__ready;
GdObject* scenetree_create_timer(GdObject* self, f64 p_time_sec, u8 p_process_always, u8 p_process_in_physics, u8 p_ignore_time_scale) {
    if !scenetree_create_timer__ready {
        scenetree_create_timer__bind = gd_method("SceneTree", "create_timer", 2709170273);
        scenetree_create_timer__ready = true;
    }
    void*[4] a;
    a[0] = cast(void*, &p_time_sec);
    a[1] = cast(void*, &p_process_always);
    a[2] = cast(void*, &p_process_in_physics);
    a[3] = cast(void*, &p_ignore_time_scale);
    GdObject* r = null;
    gd_ptrcall(scenetree_create_timer__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// scenetree_create_timer - default ignore_time_scale=false
GdObject* scenetree_create_timer(GdObject* self, f64 p_time_sec, u8 p_process_always, u8 p_process_in_physics) {
    return scenetree_create_timer(self, p_time_sec, p_process_always, p_process_in_physics, 0);
}

// scenetree_create_timer - default process_in_physics=false, ignore_time_scale=false
GdObject* scenetree_create_timer(GdObject* self, f64 p_time_sec, u8 p_process_always) {
    return scenetree_create_timer(self, p_time_sec, p_process_always, 0, 0);
}

// scenetree_create_timer - default process_always=true, process_in_physics=false, ignore_time_scale=false
GdObject* scenetree_create_timer(GdObject* self, f64 p_time_sec) {
    return scenetree_create_timer(self, p_time_sec, 1, 0, 0);
}

// SceneTree.quit(exit_code: int) -> void
void* scenetree_quit__bind;
bool scenetree_quit__ready;
void scenetree_quit(GdObject* self, i64 p_exit_code) {
    if !scenetree_quit__ready {
        scenetree_quit__bind = gd_method("SceneTree", "quit", 1995695955);
        scenetree_quit__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_exit_code);
    gd_ptrcall(scenetree_quit__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// scenetree_quit - default exit_code=0
void scenetree_quit(GdObject* self) {
    scenetree_quit(self, 0);
    return;
}

// === Shader ===

// Shader_new() -> GdObject*
GdObject* shader_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Shader");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Shader.set_code(code: String) -> void
void* shader_set_code__bind;
bool shader_set_code__ready;
void shader_set_code(GdObject* self, GdString* p_code) {
    if !shader_set_code__ready {
        shader_set_code__bind = gd_method("Shader", "set_code", 83702148);
        shader_set_code__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_code);
    gd_ptrcall(shader_set_code__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Shader.get_code() -> String
void* shader_get_code__bind;
bool shader_get_code__ready;
void shader_get_code(GdObject* self, GdString* r_ret) {
    if !shader_get_code__ready {
        shader_get_code__bind = gd_method("Shader", "get_code", 201670096);
        shader_get_code__ready = true;
    }
    gd_ptrcall(shader_get_code__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// === ShaderMaterial ===

// ShaderMaterial_new() -> GdObject*
GdObject* shadermaterial_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "ShaderMaterial");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// ShaderMaterial.set_shader(shader: Shader) -> void
void* shadermaterial_set_shader__bind;
bool shadermaterial_set_shader__ready;
void shadermaterial_set_shader(GdObject* self, GdObject* p_shader) {
    if !shadermaterial_set_shader__ready {
        shadermaterial_set_shader__bind = gd_method("ShaderMaterial", "set_shader", 3341921675);
        shadermaterial_set_shader__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_shader);
    gd_ptrcall(shadermaterial_set_shader__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// ShaderMaterial.get_shader() -> Shader
void* shadermaterial_get_shader__bind;
bool shadermaterial_get_shader__ready;
GdObject* shadermaterial_get_shader(GdObject* self) {
    if !shadermaterial_get_shader__ready {
        shadermaterial_get_shader__bind = gd_method("ShaderMaterial", "get_shader", 2078273437);
        shadermaterial_get_shader__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(shadermaterial_get_shader__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// ShaderMaterial.set_shader_parameter(param: StringName, value: Variant) -> void
void* shadermaterial_set_shader_parameter__bind;
bool shadermaterial_set_shader_parameter__ready;
void shadermaterial_set_shader_parameter(GdObject* self, GdStringName* p_param, GdVariant* p_value) {
    if !shadermaterial_set_shader_parameter__ready {
        shadermaterial_set_shader_parameter__bind = gd_method("ShaderMaterial", "set_shader_parameter", 3776071444);
        shadermaterial_set_shader_parameter__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_param);
    a[1] = cast(void*, p_value);
    gd_ptrcall(shadermaterial_set_shader_parameter__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// ShaderMaterial.get_shader_parameter(param: StringName) -> Variant
void* shadermaterial_get_shader_parameter__bind;
bool shadermaterial_get_shader_parameter__ready;
void shadermaterial_get_shader_parameter(GdObject* self, GdStringName* p_param, GdVariant* r_ret) {
    if !shadermaterial_get_shader_parameter__ready {
        shadermaterial_get_shader_parameter__bind = gd_method("ShaderMaterial", "get_shader_parameter", 2760726917);
        shadermaterial_get_shader_parameter__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_param);
    gd_ptrcall(shadermaterial_get_shader_parameter__bind, cast(void*, self), cast(void*, &a[0]), cast(void*, r_ret));
    return;
}

// === BaseMaterial3D ===

// BaseMaterial3D_new() -> GdObject*
GdObject* basematerial3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "BaseMaterial3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// BaseMaterial3D.set_albedo(albedo: Color) -> void
void* basematerial3d_set_albedo__bind;
bool basematerial3d_set_albedo__ready;
void basematerial3d_set_albedo(GdObject* self, GdColor* p_albedo) {
    if !basematerial3d_set_albedo__ready {
        basematerial3d_set_albedo__bind = gd_method("BaseMaterial3D", "set_albedo", 2920490490);
        basematerial3d_set_albedo__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_albedo);
    gd_ptrcall(basematerial3d_set_albedo__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// BaseMaterial3D.get_albedo() -> Color
void* basematerial3d_get_albedo__bind;
bool basematerial3d_get_albedo__ready;
void basematerial3d_get_albedo(GdObject* self, GdColor* r_ret) {
    if !basematerial3d_get_albedo__ready {
        basematerial3d_get_albedo__bind = gd_method("BaseMaterial3D", "get_albedo", 3444240500);
        basematerial3d_get_albedo__ready = true;
    }
    gd_ptrcall(basematerial3d_get_albedo__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// === GeometryInstance3D ===

// GeometryInstance3D_new() -> GdObject*
GdObject* geometryinstance3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "GeometryInstance3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// GeometryInstance3D.set_material_override(material: Material) -> void
void* geometryinstance3d_set_material_override__bind;
bool geometryinstance3d_set_material_override__ready;
void geometryinstance3d_set_material_override(GdObject* self, GdObject* p_material) {
    if !geometryinstance3d_set_material_override__ready {
        geometryinstance3d_set_material_override__bind = gd_method("GeometryInstance3D", "set_material_override", 2757459619);
        geometryinstance3d_set_material_override__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_material);
    gd_ptrcall(geometryinstance3d_set_material_override__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// GeometryInstance3D.get_material_override() -> Material
void* geometryinstance3d_get_material_override__bind;
bool geometryinstance3d_get_material_override__ready;
GdObject* geometryinstance3d_get_material_override(GdObject* self) {
    if !geometryinstance3d_get_material_override__ready {
        geometryinstance3d_get_material_override__bind = gd_method("GeometryInstance3D", "get_material_override", 5934680);
        geometryinstance3d_get_material_override__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(geometryinstance3d_get_material_override__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// === MeshInstance3D ===

// MeshInstance3D_new() -> GdObject*
GdObject* meshinstance3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "MeshInstance3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// MeshInstance3D.set_mesh(mesh: Mesh) -> void
void* meshinstance3d_set_mesh__bind;
bool meshinstance3d_set_mesh__ready;
void meshinstance3d_set_mesh(GdObject* self, GdObject* p_mesh) {
    if !meshinstance3d_set_mesh__ready {
        meshinstance3d_set_mesh__bind = gd_method("MeshInstance3D", "set_mesh", 194775623);
        meshinstance3d_set_mesh__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_mesh);
    gd_ptrcall(meshinstance3d_set_mesh__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// MeshInstance3D.get_mesh() -> Mesh
void* meshinstance3d_get_mesh__bind;
bool meshinstance3d_get_mesh__ready;
GdObject* meshinstance3d_get_mesh(GdObject* self) {
    if !meshinstance3d_get_mesh__ready {
        meshinstance3d_get_mesh__bind = gd_method("MeshInstance3D", "get_mesh", 1808005922);
        meshinstance3d_get_mesh__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(meshinstance3d_get_mesh__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// MeshInstance3D.set_surface_override_material(surface: int, material: Material) -> void
void* meshinstance3d_set_surface_override_material__bind;
bool meshinstance3d_set_surface_override_material__ready;
void meshinstance3d_set_surface_override_material(GdObject* self, i64 p_surface, GdObject* p_material) {
    if !meshinstance3d_set_surface_override_material__ready {
        meshinstance3d_set_surface_override_material__bind = gd_method("MeshInstance3D", "set_surface_override_material", 3671737478);
        meshinstance3d_set_surface_override_material__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, &p_surface);
    a[1] = cast(void*, &p_material);
    gd_ptrcall(meshinstance3d_set_surface_override_material__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === CSGBox3D ===

// CSGBox3D_new() -> GdObject*
GdObject* csgbox3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "CSGBox3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// CSGBox3D.set_size(size: Vector3) -> void
void* csgbox3d_set_size__bind;
bool csgbox3d_set_size__ready;
void csgbox3d_set_size(GdObject* self, GdVector3* p_size) {
    if !csgbox3d_set_size__ready {
        csgbox3d_set_size__bind = gd_method("CSGBox3D", "set_size", 3460891852);
        csgbox3d_set_size__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_size);
    gd_ptrcall(csgbox3d_set_size__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGBox3D.get_size() -> Vector3
void* csgbox3d_get_size__bind;
bool csgbox3d_get_size__ready;
void csgbox3d_get_size(GdObject* self, GdVector3* r_ret) {
    if !csgbox3d_get_size__ready {
        csgbox3d_get_size__bind = gd_method("CSGBox3D", "get_size", 3360562783);
        csgbox3d_get_size__ready = true;
    }
    gd_ptrcall(csgbox3d_get_size__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// CSGBox3D.set_material(material: Material) -> void
void* csgbox3d_set_material__bind;
bool csgbox3d_set_material__ready;
void csgbox3d_set_material(GdObject* self, GdObject* p_material) {
    if !csgbox3d_set_material__ready {
        csgbox3d_set_material__bind = gd_method("CSGBox3D", "set_material", 2757459619);
        csgbox3d_set_material__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_material);
    gd_ptrcall(csgbox3d_set_material__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGBox3D.get_material() -> Material
void* csgbox3d_get_material__bind;
bool csgbox3d_get_material__ready;
GdObject* csgbox3d_get_material(GdObject* self) {
    if !csgbox3d_get_material__ready {
        csgbox3d_get_material__bind = gd_method("CSGBox3D", "get_material", 5934680);
        csgbox3d_get_material__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(csgbox3d_get_material__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// === Sprite2D ===

// Sprite2D_new() -> GdObject*
GdObject* sprite2d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Sprite2D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Sprite2D.set_texture(texture: Texture2D) -> void
void* sprite2d_set_texture__bind;
bool sprite2d_set_texture__ready;
void sprite2d_set_texture(GdObject* self, GdObject* p_texture) {
    if !sprite2d_set_texture__ready {
        sprite2d_set_texture__bind = gd_method("Sprite2D", "set_texture", 4051416890);
        sprite2d_set_texture__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_texture);
    gd_ptrcall(sprite2d_set_texture__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Sprite2D.get_texture() -> Texture2D
void* sprite2d_get_texture__bind;
bool sprite2d_get_texture__ready;
GdObject* sprite2d_get_texture(GdObject* self) {
    if !sprite2d_get_texture__ready {
        sprite2d_get_texture__bind = gd_method("Sprite2D", "get_texture", 3635182373);
        sprite2d_get_texture__ready = true;
    }
    GdObject* r = null;
    gd_ptrcall(sprite2d_get_texture__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// === Label ===

// Label_new() -> GdObject*
GdObject* label_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Label");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Label.set_text(text: String) -> void
void* label_set_text__bind;
bool label_set_text__ready;
void label_set_text(GdObject* self, GdString* p_text) {
    if !label_set_text__ready {
        label_set_text__bind = gd_method("Label", "set_text", 83702148);
        label_set_text__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_text);
    gd_ptrcall(label_set_text__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Label.get_text() -> String
void* label_get_text__bind;
bool label_get_text__ready;
void label_get_text(GdObject* self, GdString* r_ret) {
    if !label_get_text__ready {
        label_get_text__bind = gd_method("Label", "get_text", 201670096);
        label_get_text__ready = true;
    }
    gd_ptrcall(label_get_text__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// === Timer ===

// Timer_new() -> GdObject*
GdObject* timer_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Timer");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Timer.start(time_sec: float) -> void
void* timer_start__bind;
bool timer_start__ready;
void timer_start(GdObject* self, f64 p_time_sec) {
    if !timer_start__ready {
        timer_start__bind = gd_method("Timer", "start", 1392008558);
        timer_start__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_time_sec);
    gd_ptrcall(timer_start__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// timer_start - default time_sec=-1
void timer_start(GdObject* self) {
    timer_start(self, -1.0);
    return;
}

// Timer.stop() -> void
void* timer_stop__bind;
bool timer_stop__ready;
void timer_stop(GdObject* self) {
    if !timer_stop__ready {
        timer_stop__bind = gd_method("Timer", "stop", 3218959716);
        timer_stop__ready = true;
    }
    gd_ptrcall(timer_stop__bind, cast(void*, self), null, null);
    return;
}

// Timer.set_wait_time(time_sec: float) -> void
void* timer_set_wait_time__bind;
bool timer_set_wait_time__ready;
void timer_set_wait_time(GdObject* self, f64 p_time_sec) {
    if !timer_set_wait_time__ready {
        timer_set_wait_time__bind = gd_method("Timer", "set_wait_time", 373806689);
        timer_set_wait_time__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_time_sec);
    gd_ptrcall(timer_set_wait_time__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Timer.get_wait_time() -> float
void* timer_get_wait_time__bind;
bool timer_get_wait_time__ready;
f64 timer_get_wait_time(GdObject* self) {
    if !timer_get_wait_time__ready {
        timer_get_wait_time__bind = gd_method("Timer", "get_wait_time", 1740695150);
        timer_get_wait_time__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(timer_get_wait_time__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// Timer.set_one_shot(enable: bool) -> void
void* timer_set_one_shot__bind;
bool timer_set_one_shot__ready;
void timer_set_one_shot(GdObject* self, u8 p_enable) {
    if !timer_set_one_shot__ready {
        timer_set_one_shot__bind = gd_method("Timer", "set_one_shot", 2586408642);
        timer_set_one_shot__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_enable);
    gd_ptrcall(timer_set_one_shot__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Timer.get_time_left() -> float
void* timer_get_time_left__bind;
bool timer_get_time_left__ready;
f64 timer_get_time_left(GdObject* self) {
    if !timer_get_time_left__ready {
        timer_get_time_left__bind = gd_method("Timer", "get_time_left", 1740695150);
        timer_get_time_left__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(timer_get_time_left__bind, cast(void*, self), null, cast(void*, &r));
    return r;
}

// === BoxMesh ===

// BoxMesh_new() -> GdObject*
GdObject* boxmesh_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "BoxMesh");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// BoxMesh.set_size(size: Vector3) -> void
void* boxmesh_set_size__bind;
bool boxmesh_set_size__ready;
void boxmesh_set_size(GdObject* self, GdVector3* p_size) {
    if !boxmesh_set_size__ready {
        boxmesh_set_size__bind = gd_method("BoxMesh", "set_size", 3460891852);
        boxmesh_set_size__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_size);
    gd_ptrcall(boxmesh_set_size__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === SphereMesh ===

// SphereMesh_new() -> GdObject*
GdObject* spheremesh_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "SphereMesh");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// SphereMesh.set_radius(radius: float) -> void
void* spheremesh_set_radius__bind;
bool spheremesh_set_radius__ready;
void spheremesh_set_radius(GdObject* self, f64 p_radius) {
    if !spheremesh_set_radius__ready {
        spheremesh_set_radius__bind = gd_method("SphereMesh", "set_radius", 373806689);
        spheremesh_set_radius__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_radius);
    gd_ptrcall(spheremesh_set_radius__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// SphereMesh.set_height(height: float) -> void
void* spheremesh_set_height__bind;
bool spheremesh_set_height__ready;
void spheremesh_set_height(GdObject* self, f64 p_height) {
    if !spheremesh_set_height__ready {
        spheremesh_set_height__bind = gd_method("SphereMesh", "set_height", 373806689);
        spheremesh_set_height__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_height);
    gd_ptrcall(spheremesh_set_height__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === PlaneMesh ===

// PlaneMesh_new() -> GdObject*
GdObject* planemesh_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "PlaneMesh");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// PlaneMesh.set_size(size: Vector2) -> void
void* planemesh_set_size__bind;
bool planemesh_set_size__ready;
void planemesh_set_size(GdObject* self, GdVector2* p_size) {
    if !planemesh_set_size__ready {
        planemesh_set_size__bind = gd_method("PlaneMesh", "set_size", 743155724);
        planemesh_set_size__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_size);
    gd_ptrcall(planemesh_set_size__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === StandardMaterial3D ===

// StandardMaterial3D_new() -> GdObject*
GdObject* standardmaterial3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "StandardMaterial3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// === DirectionalLight3D ===

// DirectionalLight3D_new() -> GdObject*
GdObject* directionallight3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "DirectionalLight3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// === OmniLight3D ===

// OmniLight3D_new() -> GdObject*
GdObject* omnilight3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "OmniLight3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// === SpotLight3D ===

// SpotLight3D_new() -> GdObject*
GdObject* spotlight3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "SpotLight3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// === CSGSphere3D ===

// CSGSphere3D_new() -> GdObject*
GdObject* csgsphere3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "CSGSphere3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// CSGSphere3D.set_radius(radius: float) -> void
void* csgsphere3d_set_radius__bind;
bool csgsphere3d_set_radius__ready;
void csgsphere3d_set_radius(GdObject* self, f64 p_radius) {
    if !csgsphere3d_set_radius__ready {
        csgsphere3d_set_radius__bind = gd_method("CSGSphere3D", "set_radius", 373806689);
        csgsphere3d_set_radius__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_radius);
    gd_ptrcall(csgsphere3d_set_radius__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGSphere3D.set_material(material: Material) -> void
void* csgsphere3d_set_material__bind;
bool csgsphere3d_set_material__ready;
void csgsphere3d_set_material(GdObject* self, GdObject* p_material) {
    if !csgsphere3d_set_material__ready {
        csgsphere3d_set_material__bind = gd_method("CSGSphere3D", "set_material", 2757459619);
        csgsphere3d_set_material__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_material);
    gd_ptrcall(csgsphere3d_set_material__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGSphere3D.set_radial_segments(radial_segments: int) -> void
void* csgsphere3d_set_radial_segments__bind;
bool csgsphere3d_set_radial_segments__ready;
void csgsphere3d_set_radial_segments(GdObject* self, i64 p_radial_segments) {
    if !csgsphere3d_set_radial_segments__ready {
        csgsphere3d_set_radial_segments__bind = gd_method("CSGSphere3D", "set_radial_segments", 1286410249);
        csgsphere3d_set_radial_segments__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_radial_segments);
    gd_ptrcall(csgsphere3d_set_radial_segments__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGSphere3D.set_rings(rings: int) -> void
void* csgsphere3d_set_rings__bind;
bool csgsphere3d_set_rings__ready;
void csgsphere3d_set_rings(GdObject* self, i64 p_rings) {
    if !csgsphere3d_set_rings__ready {
        csgsphere3d_set_rings__bind = gd_method("CSGSphere3D", "set_rings", 1286410249);
        csgsphere3d_set_rings__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_rings);
    gd_ptrcall(csgsphere3d_set_rings__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGSphere3D.set_smooth_faces(smooth_faces: bool) -> void
void* csgsphere3d_set_smooth_faces__bind;
bool csgsphere3d_set_smooth_faces__ready;
void csgsphere3d_set_smooth_faces(GdObject* self, u8 p_smooth_faces) {
    if !csgsphere3d_set_smooth_faces__ready {
        csgsphere3d_set_smooth_faces__bind = gd_method("CSGSphere3D", "set_smooth_faces", 2586408642);
        csgsphere3d_set_smooth_faces__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_smooth_faces);
    gd_ptrcall(csgsphere3d_set_smooth_faces__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === CSGCylinder3D ===

// CSGCylinder3D_new() -> GdObject*
GdObject* csgcylinder3d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "CSGCylinder3D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// CSGCylinder3D.set_radius(radius: float) -> void
void* csgcylinder3d_set_radius__bind;
bool csgcylinder3d_set_radius__ready;
void csgcylinder3d_set_radius(GdObject* self, f64 p_radius) {
    if !csgcylinder3d_set_radius__ready {
        csgcylinder3d_set_radius__bind = gd_method("CSGCylinder3D", "set_radius", 373806689);
        csgcylinder3d_set_radius__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_radius);
    gd_ptrcall(csgcylinder3d_set_radius__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGCylinder3D.set_height(height: float) -> void
void* csgcylinder3d_set_height__bind;
bool csgcylinder3d_set_height__ready;
void csgcylinder3d_set_height(GdObject* self, f64 p_height) {
    if !csgcylinder3d_set_height__ready {
        csgcylinder3d_set_height__bind = gd_method("CSGCylinder3D", "set_height", 373806689);
        csgcylinder3d_set_height__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_height);
    gd_ptrcall(csgcylinder3d_set_height__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGCylinder3D.set_sides(sides: int) -> void
void* csgcylinder3d_set_sides__bind;
bool csgcylinder3d_set_sides__ready;
void csgcylinder3d_set_sides(GdObject* self, i64 p_sides) {
    if !csgcylinder3d_set_sides__ready {
        csgcylinder3d_set_sides__bind = gd_method("CSGCylinder3D", "set_sides", 1286410249);
        csgcylinder3d_set_sides__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_sides);
    gd_ptrcall(csgcylinder3d_set_sides__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// CSGCylinder3D.set_material(material: Material) -> void
void* csgcylinder3d_set_material__bind;
bool csgcylinder3d_set_material__ready;
void csgcylinder3d_set_material(GdObject* self, GdObject* p_material) {
    if !csgcylinder3d_set_material__ready {
        csgcylinder3d_set_material__bind = gd_method("CSGCylinder3D", "set_material", 2757459619);
        csgcylinder3d_set_material__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_material);
    gd_ptrcall(csgcylinder3d_set_material__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// === Window ===

// Window_new() -> GdObject*
GdObject* window_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Window");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Window.set_title(title: String) -> void
void* window_set_title__bind;
bool window_set_title__ready;
void window_set_title(GdObject* self, GdString* p_title) {
    if !window_set_title__ready {
        window_set_title__bind = gd_method("Window", "set_title", 83702148);
        window_set_title__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_title);
    gd_ptrcall(window_set_title__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Window.set_size(size: Vector2i) -> void
void* window_set_size__bind;
bool window_set_size__ready;
void window_set_size(GdObject* self, GdVector2i* p_size) {
    if !window_set_size__ready {
        window_set_size__bind = gd_method("Window", "set_size", 1130785943);
        window_set_size__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_size);
    gd_ptrcall(window_set_size__bind, cast(void*, self), cast(void*, &a[0]), null);
    return;
}

// Window.get_size() -> Vector2i
void* window_get_size__bind;
bool window_get_size__ready;
void window_get_size(GdObject* self, GdVector2i* r_ret) {
    if !window_get_size__ready {
        window_get_size__bind = gd_method("Window", "get_size", 3690982128);
        window_get_size__ready = true;
    }
    gd_ptrcall(window_get_size__bind, cast(void*, self), null, cast(void*, r_ret));
    return;
}

// === Input (singleton) ===

// Input_singleton() -> GdObject*  (cached global singleton)
void* input_singleton__cached;
GdObject* input_singleton() {
    if input_singleton__cached == null {
        GdStringName sn;
        gd_stringname_new(&sn, "Input");
        input_singleton__cached = gd_global_get_singleton(cast(void*, &sn));
        gd_stringname_destroy(&sn);
    }
    return cast(GdObject*, input_singleton__cached);
}

// Input.is_action_pressed(action: StringName, exact_match: bool) -> bool
void* input_is_action_pressed__bind;
bool input_is_action_pressed__ready;
u8 input_is_action_pressed(GdStringName* p_action, u8 p_exact_match) {
    if !input_is_action_pressed__ready {
        input_is_action_pressed__bind = gd_method("Input", "is_action_pressed", 1558498928);
        input_is_action_pressed__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_action);
    a[1] = cast(void*, &p_exact_match);
    u8 r = 0;
    gd_ptrcall(input_is_action_pressed__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// input_is_action_pressed - default exact_match=false
u8 input_is_action_pressed(GdStringName* p_action) {
    return input_is_action_pressed(p_action, 0);
}

// Input.is_action_just_pressed(action: StringName, exact_match: bool) -> bool
void* input_is_action_just_pressed__bind;
bool input_is_action_just_pressed__ready;
u8 input_is_action_just_pressed(GdStringName* p_action, u8 p_exact_match) {
    if !input_is_action_just_pressed__ready {
        input_is_action_just_pressed__bind = gd_method("Input", "is_action_just_pressed", 1558498928);
        input_is_action_just_pressed__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_action);
    a[1] = cast(void*, &p_exact_match);
    u8 r = 0;
    gd_ptrcall(input_is_action_just_pressed__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// input_is_action_just_pressed - default exact_match=false
u8 input_is_action_just_pressed(GdStringName* p_action) {
    return input_is_action_just_pressed(p_action, 0);
}

// Input.is_action_just_released(action: StringName, exact_match: bool) -> bool
void* input_is_action_just_released__bind;
bool input_is_action_just_released__ready;
u8 input_is_action_just_released(GdStringName* p_action, u8 p_exact_match) {
    if !input_is_action_just_released__ready {
        input_is_action_just_released__bind = gd_method("Input", "is_action_just_released", 1558498928);
        input_is_action_just_released__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_action);
    a[1] = cast(void*, &p_exact_match);
    u8 r = 0;
    gd_ptrcall(input_is_action_just_released__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// input_is_action_just_released - default exact_match=false
u8 input_is_action_just_released(GdStringName* p_action) {
    return input_is_action_just_released(p_action, 0);
}

// Input.is_key_pressed(keycode: enum::Key) -> bool
void* input_is_key_pressed__bind;
bool input_is_key_pressed__ready;
u8 input_is_key_pressed(i64 p_keycode) {
    if !input_is_key_pressed__ready {
        input_is_key_pressed__bind = gd_method("Input", "is_key_pressed", 1938909964);
        input_is_key_pressed__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_keycode);
    u8 r = 0;
    gd_ptrcall(input_is_key_pressed__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Input.is_physical_key_pressed(keycode: enum::Key) -> bool
void* input_is_physical_key_pressed__bind;
bool input_is_physical_key_pressed__ready;
u8 input_is_physical_key_pressed(i64 p_keycode) {
    if !input_is_physical_key_pressed__ready {
        input_is_physical_key_pressed__bind = gd_method("Input", "is_physical_key_pressed", 1938909964);
        input_is_physical_key_pressed__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_keycode);
    u8 r = 0;
    gd_ptrcall(input_is_physical_key_pressed__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Input.is_mouse_button_pressed(button: enum::MouseButton) -> bool
void* input_is_mouse_button_pressed__bind;
bool input_is_mouse_button_pressed__ready;
u8 input_is_mouse_button_pressed(i64 p_button) {
    if !input_is_mouse_button_pressed__ready {
        input_is_mouse_button_pressed__bind = gd_method("Input", "is_mouse_button_pressed", 1821097125);
        input_is_mouse_button_pressed__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_button);
    u8 r = 0;
    gd_ptrcall(input_is_mouse_button_pressed__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Input.get_vector(negative_x: StringName, positive_x: StringName, negative_y: StringName, positive_y: StringName, deadzone: float) -> Vector2
void* input_get_vector__bind;
bool input_get_vector__ready;
void input_get_vector(GdStringName* p_negative_x, GdStringName* p_positive_x, GdStringName* p_negative_y, GdStringName* p_positive_y, f64 p_deadzone, GdVector2* r_ret) {
    if !input_get_vector__ready {
        input_get_vector__bind = gd_method("Input", "get_vector", 2479607902);
        input_get_vector__ready = true;
    }
    void*[5] a;
    a[0] = cast(void*, p_negative_x);
    a[1] = cast(void*, p_positive_x);
    a[2] = cast(void*, p_negative_y);
    a[3] = cast(void*, p_positive_y);
    a[4] = cast(void*, &p_deadzone);
    gd_ptrcall(input_get_vector__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, r_ret));
    return;
}

// input_get_vector - default deadzone=-1.0
void input_get_vector(GdStringName* p_negative_x, GdStringName* p_positive_x, GdStringName* p_negative_y, GdStringName* p_positive_y, GdVector2* r_ret) {
    input_get_vector(p_negative_x, p_positive_x, p_negative_y, p_positive_y, -1.0, r_ret);
    return;
}

// Input.get_axis(negative_action: StringName, positive_action: StringName) -> float
void* input_get_axis__bind;
bool input_get_axis__ready;
f64 input_get_axis(GdStringName* p_negative_action, GdStringName* p_positive_action) {
    if !input_get_axis__ready {
        input_get_axis__bind = gd_method("Input", "get_axis", 1958752504);
        input_get_axis__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_negative_action);
    a[1] = cast(void*, p_positive_action);
    f64 r = 0.0;
    gd_ptrcall(input_get_axis__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// Input.get_action_strength(action: StringName, exact_match: bool) -> float
void* input_get_action_strength__bind;
bool input_get_action_strength__ready;
f64 input_get_action_strength(GdStringName* p_action, u8 p_exact_match) {
    if !input_get_action_strength__ready {
        input_get_action_strength__bind = gd_method("Input", "get_action_strength", 801543509);
        input_get_action_strength__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_action);
    a[1] = cast(void*, &p_exact_match);
    f64 r = 0.0;
    gd_ptrcall(input_get_action_strength__bind, cast(void*, input_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// input_get_action_strength - default exact_match=false
f64 input_get_action_strength(GdStringName* p_action) {
    return input_get_action_strength(p_action, 0);
}

// Input.get_mouse_button_mask() -> bitfield::MouseButtonMask
void* input_get_mouse_button_mask__bind;
bool input_get_mouse_button_mask__ready;
i64 input_get_mouse_button_mask() {
    if !input_get_mouse_button_mask__ready {
        input_get_mouse_button_mask__bind = gd_method("Input", "get_mouse_button_mask", 2512161324);
        input_get_mouse_button_mask__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(input_get_mouse_button_mask__bind, cast(void*, input_singleton()), null, cast(void*, &r));
    return r;
}

// Input.warp_mouse(position: Vector2) -> void
void* input_warp_mouse__bind;
bool input_warp_mouse__ready;
void input_warp_mouse(GdVector2* p_position) {
    if !input_warp_mouse__ready {
        input_warp_mouse__bind = gd_method("Input", "warp_mouse", 743155724);
        input_warp_mouse__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_position);
    gd_ptrcall(input_warp_mouse__bind, cast(void*, input_singleton()), cast(void*, &a[0]), null);
    return;
}

// Input.set_mouse_mode(mode: enum::Input.MouseMode) -> void
void* input_set_mouse_mode__bind;
bool input_set_mouse_mode__ready;
void input_set_mouse_mode(i64 p_mode) {
    if !input_set_mouse_mode__ready {
        input_set_mouse_mode__bind = gd_method("Input", "set_mouse_mode", 2228490894);
        input_set_mouse_mode__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_mode);
    gd_ptrcall(input_set_mouse_mode__bind, cast(void*, input_singleton()), cast(void*, &a[0]), null);
    return;
}

// Input.get_mouse_mode() -> enum::Input.MouseMode
void* input_get_mouse_mode__bind;
bool input_get_mouse_mode__ready;
i64 input_get_mouse_mode() {
    if !input_get_mouse_mode__ready {
        input_get_mouse_mode__bind = gd_method("Input", "get_mouse_mode", 965286182);
        input_get_mouse_mode__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(input_get_mouse_mode__bind, cast(void*, input_singleton()), null, cast(void*, &r));
    return r;
}

// Input.get_last_mouse_velocity() -> Vector2
void* input_get_last_mouse_velocity__bind;
bool input_get_last_mouse_velocity__ready;
void input_get_last_mouse_velocity(GdVector2* r_ret) {
    if !input_get_last_mouse_velocity__ready {
        input_get_last_mouse_velocity__bind = gd_method("Input", "get_last_mouse_velocity", 1497962370);
        input_get_last_mouse_velocity__ready = true;
    }
    gd_ptrcall(input_get_last_mouse_velocity__bind, cast(void*, input_singleton()), null, cast(void*, r_ret));
    return;
}

// Input.action_press(action: StringName, strength: float) -> void
void* input_action_press__bind;
bool input_action_press__ready;
void input_action_press(GdStringName* p_action, f64 p_strength) {
    if !input_action_press__ready {
        input_action_press__bind = gd_method("Input", "action_press", 1713091165);
        input_action_press__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_action);
    a[1] = cast(void*, &p_strength);
    gd_ptrcall(input_action_press__bind, cast(void*, input_singleton()), cast(void*, &a[0]), null);
    return;
}

// input_action_press - default strength=1.0
void input_action_press(GdStringName* p_action) {
    input_action_press(p_action, 1.0);
    return;
}

// Input.action_release(action: StringName) -> void
void* input_action_release__bind;
bool input_action_release__ready;
void input_action_release(GdStringName* p_action) {
    if !input_action_release__ready {
        input_action_release__bind = gd_method("Input", "action_release", 3304788590);
        input_action_release__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_action);
    gd_ptrcall(input_action_release__bind, cast(void*, input_singleton()), cast(void*, &a[0]), null);
    return;
}

// === Engine (singleton) ===

// Engine_singleton() -> GdObject*  (cached global singleton)
void* engine_singleton__cached;
GdObject* engine_singleton() {
    if engine_singleton__cached == null {
        GdStringName sn;
        gd_stringname_new(&sn, "Engine");
        engine_singleton__cached = gd_global_get_singleton(cast(void*, &sn));
        gd_stringname_destroy(&sn);
    }
    return cast(GdObject*, engine_singleton__cached);
}

// Engine.get_frames_per_second() -> float
void* engine_get_frames_per_second__bind;
bool engine_get_frames_per_second__ready;
f64 engine_get_frames_per_second() {
    if !engine_get_frames_per_second__ready {
        engine_get_frames_per_second__bind = gd_method("Engine", "get_frames_per_second", 1740695150);
        engine_get_frames_per_second__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(engine_get_frames_per_second__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// Engine.get_physics_frames() -> int
void* engine_get_physics_frames__bind;
bool engine_get_physics_frames__ready;
i64 engine_get_physics_frames() {
    if !engine_get_physics_frames__ready {
        engine_get_physics_frames__bind = gd_method("Engine", "get_physics_frames", 3905245786);
        engine_get_physics_frames__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(engine_get_physics_frames__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// Engine.get_process_frames() -> int
void* engine_get_process_frames__bind;
bool engine_get_process_frames__ready;
i64 engine_get_process_frames() {
    if !engine_get_process_frames__ready {
        engine_get_process_frames__bind = gd_method("Engine", "get_process_frames", 3905245786);
        engine_get_process_frames__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(engine_get_process_frames__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// Engine.set_time_scale(time_scale: float) -> void
void* engine_set_time_scale__bind;
bool engine_set_time_scale__ready;
void engine_set_time_scale(f64 p_time_scale) {
    if !engine_set_time_scale__ready {
        engine_set_time_scale__bind = gd_method("Engine", "set_time_scale", 373806689);
        engine_set_time_scale__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_time_scale);
    gd_ptrcall(engine_set_time_scale__bind, cast(void*, engine_singleton()), cast(void*, &a[0]), null);
    return;
}

// Engine.get_time_scale() -> float
void* engine_get_time_scale__bind;
bool engine_get_time_scale__ready;
f64 engine_get_time_scale() {
    if !engine_get_time_scale__ready {
        engine_get_time_scale__bind = gd_method("Engine", "get_time_scale", 191475506);
        engine_get_time_scale__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(engine_get_time_scale__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// Engine.is_editor_hint() -> bool
void* engine_is_editor_hint__bind;
bool engine_is_editor_hint__ready;
u8 engine_is_editor_hint() {
    if !engine_is_editor_hint__ready {
        engine_is_editor_hint__bind = gd_method("Engine", "is_editor_hint", 36873697);
        engine_is_editor_hint__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(engine_is_editor_hint__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// Engine.get_frames_drawn() -> int
void* engine_get_frames_drawn__bind;
bool engine_get_frames_drawn__ready;
i64 engine_get_frames_drawn() {
    if !engine_get_frames_drawn__ready {
        engine_get_frames_drawn__bind = gd_method("Engine", "get_frames_drawn", 2455072627);
        engine_get_frames_drawn__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(engine_get_frames_drawn__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// Engine.get_physics_interpolation_fraction() -> float
void* engine_get_physics_interpolation_fraction__bind;
bool engine_get_physics_interpolation_fraction__ready;
f64 engine_get_physics_interpolation_fraction() {
    if !engine_get_physics_interpolation_fraction__ready {
        engine_get_physics_interpolation_fraction__bind = gd_method("Engine", "get_physics_interpolation_fraction", 1740695150);
        engine_get_physics_interpolation_fraction__ready = true;
    }
    f64 r = 0.0;
    gd_ptrcall(engine_get_physics_interpolation_fraction__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// Engine.set_max_fps(max_fps: int) -> void
void* engine_set_max_fps__bind;
bool engine_set_max_fps__ready;
void engine_set_max_fps(i64 p_max_fps) {
    if !engine_set_max_fps__ready {
        engine_set_max_fps__bind = gd_method("Engine", "set_max_fps", 1286410249);
        engine_set_max_fps__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, &p_max_fps);
    gd_ptrcall(engine_set_max_fps__bind, cast(void*, engine_singleton()), cast(void*, &a[0]), null);
    return;
}

// Engine.get_max_fps() -> int
void* engine_get_max_fps__bind;
bool engine_get_max_fps__ready;
i64 engine_get_max_fps() {
    if !engine_get_max_fps__ready {
        engine_get_max_fps__bind = gd_method("Engine", "get_max_fps", 3905245786);
        engine_get_max_fps__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(engine_get_max_fps__bind, cast(void*, engine_singleton()), null, cast(void*, &r));
    return r;
}

// === Time (singleton) ===

// Time_singleton() -> GdObject*  (cached global singleton)
void* time_singleton__cached;
GdObject* time_singleton() {
    if time_singleton__cached == null {
        GdStringName sn;
        gd_stringname_new(&sn, "Time");
        time_singleton__cached = gd_global_get_singleton(cast(void*, &sn));
        gd_stringname_destroy(&sn);
    }
    return cast(GdObject*, time_singleton__cached);
}

// Time.get_ticks_msec() -> int
void* time_get_ticks_msec__bind;
bool time_get_ticks_msec__ready;
i64 time_get_ticks_msec() {
    if !time_get_ticks_msec__ready {
        time_get_ticks_msec__bind = gd_method("Time", "get_ticks_msec", 3905245786);
        time_get_ticks_msec__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(time_get_ticks_msec__bind, cast(void*, time_singleton()), null, cast(void*, &r));
    return r;
}

// Time.get_ticks_usec() -> int
void* time_get_ticks_usec__bind;
bool time_get_ticks_usec__ready;
i64 time_get_ticks_usec() {
    if !time_get_ticks_usec__ready {
        time_get_ticks_usec__bind = gd_method("Time", "get_ticks_usec", 3905245786);
        time_get_ticks_usec__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(time_get_ticks_usec__bind, cast(void*, time_singleton()), null, cast(void*, &r));
    return r;
}

// === OS (singleton) ===

// OS_singleton() -> GdObject*  (cached global singleton)
void* os_singleton__cached;
GdObject* os_singleton() {
    if os_singleton__cached == null {
        GdStringName sn;
        gd_stringname_new(&sn, "OS");
        os_singleton__cached = gd_global_get_singleton(cast(void*, &sn));
        gd_stringname_destroy(&sn);
    }
    return cast(GdObject*, os_singleton__cached);
}

// OS.get_name() -> String
void* os_get_name__bind;
bool os_get_name__ready;
void os_get_name(GdString* r_ret) {
    if !os_get_name__ready {
        os_get_name__bind = gd_method("OS", "get_name", 201670096);
        os_get_name__ready = true;
    }
    gd_ptrcall(os_get_name__bind, cast(void*, os_singleton()), null, cast(void*, r_ret));
    return;
}

// OS.has_feature(tag_name: String) -> bool
void* os_has_feature__bind;
bool os_has_feature__ready;
u8 os_has_feature(GdString* p_tag_name) {
    if !os_has_feature__ready {
        os_has_feature__bind = gd_method("OS", "has_feature", 3927539163);
        os_has_feature__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_tag_name);
    u8 r = 0;
    gd_ptrcall(os_has_feature__bind, cast(void*, os_singleton()), cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// OS.get_processor_count() -> int
void* os_get_processor_count__bind;
bool os_get_processor_count__ready;
i64 os_get_processor_count() {
    if !os_get_processor_count__ready {
        os_get_processor_count__bind = gd_method("OS", "get_processor_count", 3905245786);
        os_get_processor_count__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(os_get_processor_count__bind, cast(void*, os_singleton()), null, cast(void*, &r));
    return r;
}

// OS.get_process_id() -> int
void* os_get_process_id__bind;
bool os_get_process_id__ready;
i64 os_get_process_id() {
    if !os_get_process_id__ready {
        os_get_process_id__bind = gd_method("OS", "get_process_id", 3905245786);
        os_get_process_id__ready = true;
    }
    i64 r = 0;
    gd_ptrcall(os_get_process_id__bind, cast(void*, os_singleton()), null, cast(void*, &r));
    return r;
}

// OS.get_processor_name() -> String
void* os_get_processor_name__bind;
bool os_get_processor_name__ready;
void os_get_processor_name(GdString* r_ret) {
    if !os_get_processor_name__ready {
        os_get_processor_name__bind = gd_method("OS", "get_processor_name", 201670096);
        os_get_processor_name__ready = true;
    }
    gd_ptrcall(os_get_processor_name__bind, cast(void*, os_singleton()), null, cast(void*, r_ret));
    return;
}

// OS.is_debug_build() -> bool
void* os_is_debug_build__bind;
bool os_is_debug_build__ready;
u8 os_is_debug_build() {
    if !os_is_debug_build__ready {
        os_is_debug_build__bind = gd_method("OS", "is_debug_build", 36873697);
        os_is_debug_build__ready = true;
    }
    u8 r = 0;
    gd_ptrcall(os_is_debug_build__bind, cast(void*, os_singleton()), null, cast(void*, &r));
    return r;
}

// OS.get_locale() -> String
void* os_get_locale__bind;
bool os_get_locale__ready;
void os_get_locale(GdString* r_ret) {
    if !os_get_locale__ready {
        os_get_locale__bind = gd_method("OS", "get_locale", 201670096);
        os_get_locale__ready = true;
    }
    gd_ptrcall(os_get_locale__bind, cast(void*, os_singleton()), null, cast(void*, r_ret));
    return;
}

// === JSON ===

// JSON_new() -> GdObject*
GdObject* json_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "JSON");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// JSON.stringify(data: Variant, indent: String, sort_keys: bool, full_precision: bool) -> String  [static]
void* json_stringify__bind;
bool json_stringify__ready;
void json_stringify(GdVariant* p_data, GdString* p_indent, u8 p_sort_keys, u8 p_full_precision, GdString* r_ret) {
    if !json_stringify__ready {
        json_stringify__bind = gd_method("JSON", "stringify", 462733549);
        json_stringify__ready = true;
    }
    void*[4] a;
    a[0] = cast(void*, p_data);
    a[1] = cast(void*, p_indent);
    a[2] = cast(void*, &p_sort_keys);
    a[3] = cast(void*, &p_full_precision);
    gd_ptrcall(json_stringify__bind, null, cast(void*, &a[0]), cast(void*, r_ret));
    return;
}

// json_stringify - default full_precision=false
void json_stringify(GdVariant* p_data, GdString* p_indent, u8 p_sort_keys, GdString* r_ret) {
    json_stringify(p_data, p_indent, p_sort_keys, 0, r_ret);
    return;
}

// json_stringify - default sort_keys=true, full_precision=false
void json_stringify(GdVariant* p_data, GdString* p_indent, GdString* r_ret) {
    json_stringify(p_data, p_indent, 1, 0, r_ret);
    return;
}

// json_stringify - default indent="", sort_keys=true, full_precision=false
void json_stringify(GdVariant* p_data, GdString* r_ret) {
    GdString _d1;
    gd_string_new(&_d1, "");
    json_stringify(p_data, &_d1, 1, 0, r_ret);
    gd_string_destroy(&_d1);
    return;
}

// JSON.parse_string(json_string: String) -> Variant  [static]
void* json_parse_string__bind;
bool json_parse_string__ready;
void json_parse_string(GdString* p_json_string, GdVariant* r_ret) {
    if !json_parse_string__ready {
        json_parse_string__bind = gd_method("JSON", "parse_string", 309047738);
        json_parse_string__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_json_string);
    gd_ptrcall(json_parse_string__bind, null, cast(void*, &a[0]), cast(void*, r_ret));
    return;
}

// === FileAccess ===

// FileAccess_new() -> GdObject*
GdObject* fileaccess_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "FileAccess");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// FileAccess.file_exists(path: String) -> bool  [static]
void* fileaccess_file_exists__bind;
bool fileaccess_file_exists__ready;
u8 fileaccess_file_exists(GdString* p_path) {
    if !fileaccess_file_exists__ready {
        fileaccess_file_exists__bind = gd_method("FileAccess", "file_exists", 2323990056);
        fileaccess_file_exists__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_path);
    u8 r = 0;
    gd_ptrcall(fileaccess_file_exists__bind, null, cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// FileAccess.get_file_as_string(path: String) -> String  [static]
void* fileaccess_get_file_as_string__bind;
bool fileaccess_get_file_as_string__ready;
void fileaccess_get_file_as_string(GdString* p_path, GdString* r_ret) {
    if !fileaccess_get_file_as_string__ready {
        fileaccess_get_file_as_string__bind = gd_method("FileAccess", "get_file_as_string", 1703090593);
        fileaccess_get_file_as_string__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_path);
    gd_ptrcall(fileaccess_get_file_as_string__bind, null, cast(void*, &a[0]), cast(void*, r_ret));
    return;
}

// FileAccess.open(path: String, flags: enum::FileAccess.ModeFlags) -> FileAccess  [static]
void* fileaccess_open__bind;
bool fileaccess_open__ready;
GdObject* fileaccess_open(GdString* p_path, i64 p_flags) {
    if !fileaccess_open__ready {
        fileaccess_open__bind = gd_method("FileAccess", "open", 1247358404);
        fileaccess_open__ready = true;
    }
    void*[2] a;
    a[0] = cast(void*, p_path);
    a[1] = cast(void*, &p_flags);
    GdObject* r = null;
    gd_ptrcall(fileaccess_open__bind, null, cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// === DirAccess ===

// DirAccess_new() -> GdObject*
GdObject* diraccess_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "DirAccess");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// DirAccess.dir_exists_absolute(path: String) -> bool  [static]
void* diraccess_dir_exists_absolute__bind;
bool diraccess_dir_exists_absolute__ready;
u8 diraccess_dir_exists_absolute(GdString* p_path) {
    if !diraccess_dir_exists_absolute__ready {
        diraccess_dir_exists_absolute__bind = gd_method("DirAccess", "dir_exists_absolute", 2323990056);
        diraccess_dir_exists_absolute__ready = true;
    }
    void*[1] a;
    a[0] = cast(void*, p_path);
    u8 r = 0;
    gd_ptrcall(diraccess_dir_exists_absolute__bind, null, cast(void*, &a[0]), cast(void*, &r));
    return r;
}

// === auto-pulled dependencies ===

// Referenced by a bound method's arg/return but not in the spec.
// Accessor only (no methods); list the class explicitly to bind its methods.

// Material_new() -> GdObject*
GdObject* material_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Material");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Mesh_new() -> GdObject*
GdObject* mesh_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Mesh");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}

// Texture2D_new() -> GdObject*
GdObject* texture2d_new() {
    GdStringName sn;
    gd_stringname_new(&sn, "Texture2D");
    GdObject* obj = cast(GdObject*, gd_construct_object(cast(void*, &sn)));
    gd_stringname_destroy(&sn);
    return obj;
}
